# Set these as Environment Variables in Vercel. Never commit the real values.
TELEGRAM_BOT_TOKEN=123456789:replace_with_your_bot_token
TELEGRAM_CHAT_ID=-1001234567890

# Optional: needed only when the website and the /api/contact endpoint use different domains.
# Separate multiple domains with commas.
ALLOWED_ORIGINS=https://your-site.example
